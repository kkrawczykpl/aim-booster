class Target {
    private scale: number;
    private size: number;
    constructor(
        scale: number = 0,
        size: number = 70,
        
    ) {
        this.scale = scale;
        this.size = size;
    }
}